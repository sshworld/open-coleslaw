{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-09-29/server.schema.json",
  "name": "open-coleslaw",
  "title": "Open Coleslaw — Multi-Agent Orchestrator",
  "description": "Hierarchical multi-agent orchestration plugin for Claude Code. Like coleslaw — it's already prepared, just scoop and eat.",
  "version": "0.1.0",
  "locals": [
    {
      "type": "stdio",
      "command": "bash",
      "args": ["-c", "cd \"${CLAUDE_PLUGIN_ROOT}\" && [ -f dist/index.js ] || npm run build && node dist/index.js"]
    }
  ]
}
